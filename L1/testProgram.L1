(:go
  (:go
    0 0
    r8 <- 0
    r10 <- rax 
    rax <- :testFunction
    rdx <- mem rdi 8
    rdx += -1
    mem rdi 8 -= rcx
    call :testFunction 0
    call print 1
    call input 0 
    call tensor-error 1
    rsi++ 
    rdx--
    rax @ rdi rsi 4
    return
  )

  (:testFunction
    1 2
    return 
  )
)
