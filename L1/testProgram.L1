(:go
  (:go
    0 0
    rax <- rdi
    rdx <- mem rdi 8
    rdx += -1
    mem rdi 8 -= rcx
    call :testFunction 0
    call print 1
    call input 0 
    call tensor-error 1
    return
  )

  (:testFunction
    1 2
    return 
  )
)
